# 비디오 프레임 시간 순서 예측 대회 — 프로젝트 계획

> 이 문서는 Claude Code가 작업을 진행하기 위한 마스터 플랜이다.
> 각 Phase는 순서대로 진행하되, Phase 0의 확인 사항이 완료되기 전에는 모델 학습에 착수하지 않는다.

---

## 1. 문제 정의

- 입력: 뒤섞인 4개의 비디오 프레임 이미지 + 해당 비디오에 대한 텍스트 캡션(영어, 이벤트를 시간 순서대로 서술: "..., then ..., finally ...")
- 출력: 각 프레임의 원본 비디오에서의 위치를 나열한 순열. 예: 원본 a,b,c,d / 입력 a,d,b,c → 정답 [1, 4, 2, 3]
- 데이터: `train/`, `test/` 이미지 폴더 + 메타데이터 CSV (샘플별 고유 Id). 별도 validation 없음 → train에서 직접 분리
- 노이즈: 검은 프레임 등 캡션과 무관한 프레임, 프레임만으로 정답을 특정할 수 없는 샘플 포함. **전처리가 본선 평가 기준에 포함됨**

## 2. 대회 규칙 제약 (설계를 결정하는 조건)

| 규칙 | 설계에 미치는 영향 |
|---|---|
| 인터넷 차단 로컬 환경에서 실행 | 모든 모델 가중치 사전 다운로드, 오프라인 로딩 코드 필수 |
| 학습·추론에 외부 상용 API 금지 | 캡셔닝/추론 전부 로컬 모델. API는 학습 데이터 전처리에만 (3만 원 한도, 사용 시 문서화) |
| 외부 데이터 학습 금지 | ActivityNet 등 외부 증강 불가. **제공 데이터 내 순열 증강이 핵심 증강 수단** |
| 오픈소스 모델은 2026-05-31 이전 가중치 공개분만 | 모델 선정 시 공개일 확인·기록 |
| **모델 앙상블 금지** (분할 파인튜닝 조합도 금지) | 단일 모델 end-to-end. 서로 다른 모델의 직렬 파이프라인도 리스크 → 단일 VLM으로 통합 |
| LoRA 등 경량화 허용 | 7B급 VLM + LoRA SFT가 주 전략 |
| CoT, multi-turn, TTA 등 추론 전략 허용 | 단일 모델 내 CoT·TTA·pairwise 브루트포스는 합법 |
| 테스트 전체 추론 24시간 이내 | TTA/pairwise 횟수 예산 계산 필요 |

## 3. 핵심 전략 요약

**단일 멀티이미지 VLM(7B급) + LoRA SFT + CoT 출력 + 순열 증강 + TTA.**

- "이미지 설명 → 순서 추론"의 2모델 파이프라인 대신, 한 VLM의 CoT 안에서 "각 프레임 서술 → 캡션 이벤트와 매핑 → 순서 결론"을 수행 (정보 병목·앙상블 해석 리스크 회피)
- 전처리(노이즈 탐지·정제)는 별도 정량 리포트로 문서화하여 본선 평가에 대응

## 4. Phase 0 — 착수 전 확인 (블로커)

- [x] **정답 인코딩 방향 확정**: baseline 노트북의 `parse_model_output` 변환 코드로 확정 — **제출 라벨 = "입력 프레임 i의 시간 순위"** (rank 표현). 상세 근거는 `docs/label_encoding.md`
- [x] **평가 지표 확인**: **Exact Match Accuracy 공식 확정** (Public 70%/Private 30%, 부분점수 없음) — `docs/rules.md` §2 (2026-07-04)
- [ ] **Kaggle Discussion 사전 문의** (필요 시): ① 전처리용 CLIP/SigLIP 등 보조 모델의 학습 데이터 정제 사용 허용 범위 ② 직렬 파이프라인의 앙상블 해석 여부 (단일 VLM로 가면 불필요하지만 안전장치)
- [x] **로컬 GPU 사양 확인**: 로컬 = CPU 전용(Core Ultra 7 258V, CUDA 없음) → **학습·추론 GPU는 Kaggle 무료 노트북(주 30h, T4)** 사용으로 확정. 실행 계획은 `.claude/plans` 승인안(2-트랙) 참조 (2026-07-04)

## 5. Phase 1 — EDA & 데이터 파이프라인

`src/data/` 에 구현:

1. CSV 스키마 파악, Id ↔ 이미지 폴더 매핑 로더
2. 기본 통계: 샘플 수, 이미지 해상도 분포, 캡션 길이·언어, 라벨(순열) 분포 — 24개 순열이 균등한지 확인 (불균등하면 셔플 편향 존재 → 증강으로 교정)
   - **[확인됨]** train 9,535 / test 819. **`No_ordering=True`(15.5%)는 전부 Answer=[1,2,3,4]이고, orderable 샘플에는 identity가 0건** → orderable 이진 분류가 ~15%p를 좌우. 나머지 23개 순열은 균등(~360개). 상세는 `docs/findings.md`
3. 캡션 구조 분석: "then / after / finally / before" 등 순서 연결어 빈도, 절 개수 분포 (4개 이벤트 서술과 프레임 4장이 대응되는지)
4. **train/val 분리**: 캡션 유사도 기반 중복 제거 후 분리 (동일 비디오 유래 샘플이 train/val에 갈라지지 않도록). 기본 90/10

## 6. Phase 2 — 전처리 (본선 평가 대상, 정량화 필수)

`src/preprocess/` 에 구현. 각 단계는 **적용 전/후 지표를 리포트로 남긴다** (`reports/preprocessing.md`):

1. **검은/저정보 프레임 탐지**: 픽셀 표준편차·엔트로피 임계값. 탐지 시 해당 샘플 플래그
2. **근접 중복 프레임 탐지**: perceptual hash (phash) 거리. 순서가 본질적으로 애매한 샘플 플래그
3. **캡션-프레임 정합성 점수**: 로컬 SigLIP/CLIP으로 프레임별 캡션 유사도. 4프레임 모두 낮으면 노이즈 샘플 후보
4. **학습 데이터 처리 정책**: 노이즈 플래그 샘플은 (a) 제외, (b) loss 가중치 하향, (c) 그대로 — 세 가지를 소규모 실험으로 비교 후 선택
5. (선택) 외부 API 전처리: 한도 3만 원 내에서 라벨 오류 의심 샘플 검수 등에만. 사용 시 비용·방식 기록

## 7. Phase 3 — 베이스라인

빠르게 상한/하한 감 잡기. `src/baselines/`:

1. **Random**: 기대 성능 하한
2. **캡션-이벤트 정렬 (zero-shot, 학습 없음)**: 캡션을 연결어 기준으로 이벤트 절 분리 → SigLIP으로 프레임-이벤트 매칭 → 이벤트 순서로 프레임 정렬. *주의: 이 방식을 최종 제출에 SigLIP+VLM 결합 형태로 쓰면 앙상블 해석 리스크가 있으므로 베이스라인/분석용으로만 사용*
3. **VLM zero-shot**: 후보 VLM에 4프레임+캡션 프롬프트, 순서 직접 출력. 파인튜닝 전 성능 확인 겸 모델 선정 근거

## 8. Phase 4 — 메인 모델: VLM + LoRA SFT

`src/train/`:

1. **모델 선정**: Qwen2.5-VL-7B-Instruct 또는 InternVL 계열 (멀티이미지 입력 지원, 2026-05-31 이전 공개 확인, 라이선스 확인). zero-shot 베이스라인 성능으로 최종 결정. 가중치 로컬 저장 (`models/`)
2. **입력 포맷**: `[Frame 1] <img> [Frame 2] <img> [Frame 3] <img> [Frame 4] <img>\nCaption: {caption}\n...`
3. **출력 포맷 두 가지 실험**:
   - (a) 순열 직접 출력: `Answer: [2, 1, 4, 3]`
   - (b) 구조화 CoT: 프레임별 한 줄 서술 → 캡션 이벤트 나열 → 프레임-이벤트 매핑 → 최종 순열. CoT 타깃은 train 라벨로부터 규칙 기반 자동 생성 (캡션 절 분리 + 정답 순서 결합)
4. **순열 증강 (필수)**: 매 epoch 각 샘플의 프레임 입력 순서를 재셔플하고 라벨을 그에 맞게 변환 → 위치 편향 제거. 인코딩 방향 버그가 나기 쉬운 지점이므로 변환 함수에 단위 테스트 작성 (`tests/test_permutation.py`)
5. **학습**: LoRA (r=16~64 스윕), bf16, 전처리 정제본 사용. val 지표로 체크포인트 선택
6. **실험 관리**: 설정은 yaml (`configs/`), 결과는 `reports/experiments.md`에 누적

## 9. Phase 5 — 추론 전략 (단일 모델 내에서만)

`src/infer/`:

1. **기본**: 학습된 출력 포맷대로 생성 → 순열 파싱. 파싱 실패 시 fallback (재생성 1회 → 그래도 실패 시 [1,2,3,4])
2. **TTA**: 입력 프레임 배치 순서 K회 셔플 → 각 출력을 원좌표로 역변환 → 다수결. K는 추론 예산으로 결정
3. **(대안) Pairwise 모드**: 동일 모델에 12개 순서쌍 질의 → 24개 순열 전수 스코어링 → 최적 순열. TTA 방식과 val에서 비교해 **하나만** 최종 채택
4. **추론 예산 검증**: `test 샘플 수 × 샘플당 호출 수 × 호출당 시간 ≤ 20시간(버퍼 4시간)` 을 실측으로 확인하는 스크립트 (`src/infer/budget_check.py`)
5. 출력 검증: 모든 행이 {1,2,3,4}의 순열인지 제출 전 assert

## 10. Phase 6 — 검증·제출·발표 준비

1. val에서 오류 분석: 어떤 유형(중복 프레임, 노이즈, 긴 캡션)에서 틀리는지 분류 → 전처리·프롬프트 개선 루프
2. 제출 파일 생성기 + 포맷 검증기
3. 발표 자료용 정리: 전처리 ablation 표, 외부 API 사용 내역(사용 시), 모델 공개일·라이선스 근거, 추론 시간 실측

## 11. 프로젝트 구조

```
project/
├── PLAN.md                  # 본 문서
├── configs/                 # 학습·추론 yaml
├── data/                    # (gitignore) 대회 데이터
├── models/                  # (gitignore) 사전 다운로드 가중치
├── src/
│   ├── data/                # 로더, split
│   ├── preprocess/          # 노이즈 탐지·정제
│   ├── baselines/
│   ├── train/               # LoRA SFT
│   ├── infer/               # TTA/pairwise, budget check
│   └── utils/               # 순열 변환, 파싱, 지표
├── tests/                   # 순열 변환·파싱 단위 테스트
├── reports/                 # preprocessing.md, experiments.md
└── docs/                    # label_encoding.md 등 확정 사항 기록
```

## 12. 작업 우선순위 (Claude Code 실행 순서)

1. Phase 0 확인 사항 해소 (인코딩·지표) — **여기가 끝나기 전 학습 금지**
2. 데이터 로더 + EDA 노트북/스크립트
3. 순열 변환 유틸 + 단위 테스트 (모든 후속 작업의 기반)
4. 전처리 모듈 + 리포트
5. 베이스라인 3종 실행, 성능 기록
6. VLM zero-shot 비교 → 모델 확정 → LoRA SFT
7. 추론 전략 비교 (TTA vs pairwise) → 예산 검증 → 제출 파이프라인
8. 오류 분석 루프 + 발표 자료 정리

## 13. 리스크 및 대응

| 리스크 | 대응 |
|---|---|
| 라벨 인코딩 방향 오해 | Phase 0에서 실데이터 대조로 확정, 문서화, 단위 테스트 |
| VLM 위치 편향 (첫 프레임=시작 가정) | 순열 증강 + TTA |
| 앙상블 규칙 위반 해석 | 최종 파이프라인은 단일 VLM만. 보조 모델은 학습 데이터 전처리로 한정, 불확실하면 Discussion 문의 |
| 추론 24시간 초과 | budget_check로 사전 실측, K·CoT 길이 조정, vLLM 등 서빙 최적화 |
| GPU 메모리 부족 | 7B → 4bit QLoRA, 해상도 다운스케일 |
| CoT 출력 파싱 실패 | 구조화 출력 강제 + fallback 로직 |
